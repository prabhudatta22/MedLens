services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: medlens
      POSTGRES_PASSWORD: medlens
      POSTGRES_DB: medlens
    ports:
      - "5432:5432"
    volumes:
      - medlens_pg:/var/lib/postgresql/data

volumes:
  medlens_pg:
